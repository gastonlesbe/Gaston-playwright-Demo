[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

markers =
    ui: UI tests driven by Playwright
    api: API tests (requests, no browser)
    smoke: quick smoke checks

addopts = --html=reports/report.html --self-contained-html
